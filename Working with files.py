# -*- coding: utf-8 -*-
"""Лещенко Олег - Робота з файлами.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1Y5ge4rEhmYgeCAdue-Pjuna-tbVoUsg5

**Файл** - лінійна структура даних, розміщена у зовнішній пам'яті.

Файли, з якими відбувається робота в Python, можуть бути текстовими та бінарними (байтовими). **Текстові файли** - набір символів, поданих у деякій системі кодування. **Бінарні файли** - набір байтів, які відповідають певному алгоритму кодування даних, пов'язаному з типом даних (наприклад, алгоритму кодування графічних зображень).

З файлами можна виконувати операції **читання**, **запису** (створення або перезапису), **дозапису** (створення або додавання даних). 

Перш ніж виконувати операції, файл потрібно **відкрити**.

*ім'я_змінної = open('ім'я_файлу', 'режим')*

Режим обирають залежно від операції, яку планують виконати:
* 'r' - відкриття для читання (є значенням за промовчанням)
* 'w' - відкриття для запис, вміст файлу видаляється, якщо файлу немає, створюється новий
* 'a' - відкриття для дозапису, дані додаються до кінця файлу
* 'b' - відкриття у двійковому режимі
* 't' - відкриття в текстовому режимі (за замовчуванням)
* '+' - відкриття на читання та запис

Запис даних рядкового типу в файл - метод *write*. Для початку запису в новий рядок файлу використовують символ *'\n'*.

Після завершення роботи з файлом його рекомендується закривати.

*ім'я_змінної.close()*
"""

f = open('file.txt', 'wt')
f.write('1. Hello World!')
f.write('2. Hello World!')
f.close()

"""**Читання з файлу**

Методи читання з файлу:
* read(n) - читає n символів із файлу або всі символи (якщо аргумент n відсутній); результат - рядок 
* readline(n) - читає n символів рядка або весь рядок (якщо аргумент n відсутній); результат - рядок 
* readlines() - читає всі рядки; результат - список рядків


"""

f = open('file.txt', 'r')
text = f.read()
print(text)
f.close()

"""**Опрацювання прочитаних даних**

Методи опрацювання рядків:
* *strip()* - видаляє недруковані символи: пробіли та табуляції на початку та в кінці рядка, перенесення рядка ('\n') 
* *split()* - розділяє рядок на місці пробілу, утворює список
"""

f = open('file.txt', 'r')


f.close()

"""**Задача 1.** Згенерувати 10 цілих випадкових чисел від 1 до 10. Записати їх у текстовий файл task.txt. 

1. Відкрити файл для запису
2. Виконати 10 разів: 
- згенерувати ціле число
- перевести число в рядковий тип, записати в файл через пробіл
3. Закрити файл



"""

from random import randint
f = open('task.txt', 'w')
for _ in range(10):
  x = randint(1, 10)
  f.write(str(x)+ " ") 
f.close()

"""**Задача 2.** Прочитати числа з текстового файлу task.txt. Вивести отримані числа. Знайти та вивести їх суму. 

1. Відкрити файл для читання
2. Прочитати рядок з файлу
3. Розділити рядок на окремі слова на місцях пробілів, перетворити на список чисел
4. Знайти суму елементів списку
5. Вивести список та суму його елементів.
"""

f = open('task.txt', 'r')
text = f.read()
text=text.split()
result = [int(i) for i in text]
print(result) 
print(sum(result))
f.close()